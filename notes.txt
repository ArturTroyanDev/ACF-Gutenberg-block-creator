1. Choise to use pascalcase or BEM
2. Choise the path for creating a php folder of the block and scss file. Maybe it will be convinintent just 
set values in create-block.js. Like paths, structure etc







// що б трохи прискорити процес розробки написав невеликий скрипт для автоматизації рутинного створення папок з файлами 
// в /template-parts/blocks, 
// /sass/blocks, 
// імпорту створенного файлу в sass/blocks/_blocks.scss та реєстрації в functions.php(або у вашій конкретній директорії) 

// make this script check if such a directory is alredy exist. Or ask: "U sure u wanna rewrite the files?"
// make autosave for files changed by this script files like a function.php 
// Make it write properly values in block.json. For example value for property title should be in capital letters
// if there is no func where we have to register blocks create it
// make it to ask where to register func and scss
// make possibility to enable including data-unique id




// create-block 2 - added most common fields:

// $title = get_field("title");
// $subtitle = get_field("subtitle");
// $text = get_field("text");
// $background = get_field("background");
// $items = get_field("items");
// $link = get_field("link");
